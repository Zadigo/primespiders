$kind: http-request
url: "{{fastapi}}spiders"
method: GET
scripts:
  - type: afterResponse
    code: |-
      pm.test("get spiders", () => {
          pm.expect(pm.response.code).to.equal(200)
          pm.environment.set('spiderId', pm.response.json()[0].name)
      })
    language: text/javascript
order: 1789932814621
